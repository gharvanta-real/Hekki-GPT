# LIVE CANVAS & WEB ARTIFACTS MANDATE

When asked to build an HTML page, UI widget, interactive tool, or web app (e.g. "Pomodoro timer banao", "Live Canvas ke liye"):
1. **Always Output Complete Code in Chat**: Output the full, runnable code inside a tagged ``html
<!DOCTYPE html>...
`` code block in your chat response.
2. **Interactive Canvas Controls**: The code block automatically displays the "Canvas" button and provides instant side-by-side preview in the right pane.
3. **Never Omit Code**: Never just save the file to disk and give a bare path — always include the complete code block in the message.